# Defaults for agate initscript
# sourced by /etc/init.d/agate
# installed at /etc/default/agate by the maintainer scripts

# pulled in from the init script; makes things easier.
NAME=agate

# location of java
JAVA=/usr/bin/java
JVM_DIR=`ls /usr/lib/jvm/ | grep java-1.8`
if [ -n "$JVM_DIR" ]
then
	JAVA=/usr/lib/jvm/$JVM_DIR/bin/java
fi

# ulimit
# Increase maximum open files for the agate process
# Comment this out to use the system default
MAXOPENFILES=16384

# user id (or id:gid) to be run as
AGATE_USER=agate

# location of the agate distribution file
AGATE_DIST=/usr/share/agate

# Agate home location
AGATE_HOME=/var/lib/agate

# Agate log directory or syslog facility
AGATE_LOG=/var/log/agate

# arguments to pass to agate.
AGATE_ARGS=""

# arguments to pass to java
JAVA_ARGS="-Xms1G -Xmx2G -XX:+UseG1GC"