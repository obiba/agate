server:
    port: 8080
    address: localhost

spring:
    profiles: prod

    mongodb:
        url: localhost:27017
        databaseName: agate
        username:
        password:

    thymeleaf:
        mode: XHTML
        cache: true

shiro:
  password:
    nbHashIterations: 10000
    salt: agate

metrics:
    jmx.enabled: true
    graphite:
        enabled: false
        host: localhost
        port: 2003

cache:
    timeToLiveSeconds: 3600